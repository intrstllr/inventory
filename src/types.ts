export interface ItemInventoryType {
  image: string;
  count: number;
  id: number;
}

export interface DataStorageType {
  image: string;
  count: number;
  id: number;
  indexRow: number;
  indexCol: number;
}
